<div class="admin-countries-new-page">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <div class="title">
          <h2>Add new country</h2>
        </div>
        <div class="body-content">
          <div class="card">
            <div class="card-body">
              <%= form_for(@country, url: admin_countries_path, method: :post) do |f| %>
                <%= render 'shared/errors', object: @country %>
                <%= render 'admin/countries/form', f: f %>
                <div class="form-group">
                  <%= f.submit "Create", class: "btn btn-block btn-success" %>
                  <%= link_to "Back", admin_countries_path, class: "btn btn-block btn-link" %>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
